{
  "name": "Chatwoot Development Codespace",
  "service": "app",
  "dockerComposeFile": "docker-compose.yml",
  "features": {
    "ghcr.io/devcontainers/features/sshd:1": {
      "version": "latest"
    }
  },
  "settings": {
    "terminal.integrated.shell.linux": "/bin/zsh",
    "extensions.showRecommendationsOnlyOnDemand": true,
    "editor.formatOnSave": true,
    "files.trimTrailingWhitespace": true,
    "files.insertFinalNewline": true,
    "search.exclude": {
      "**/node_modules": true,
      "**/tmp": true,
      "**/log": true,
      "**/coverage": true,
      "**/public/packs": true
    }
  },
  "extensions": [
    "Shopify.ruby-lsp",
    "misogi.ruby-rubocop",
    "davidpallinder.rails-test-runner",
    "github.copilot",
    "mrmlnc.vscode-duplicate"
  ],
  "forwardPorts": [8025, 3000, 3036],
  "postCreateCommand": ".devcontainer/scripts/setup.sh && until pg_isready -h localhost -U postgres 2>/dev/null; do sleep 1; done && POSTGRES_STATEMENT_TIMEOUT=600s bundle exec rake db:chatwoot_prepare && pnpm install",
  "postStartCommand": "until pg_isready -h localhost -U postgres 2>/dev/null; do sleep 1; done && POSTGRES_STATEMENT_TIMEOUT=600s bundle exec rake db:chatwoot_prepare 2>/dev/null || true && bin/start-all",
  "portsAttributes": {
    "3000": {
      "label": "Rails Server",
      "visibility": "public"
    },
    "3036": {
      "label": "Vite Dev Server",
      "visibility": "public"
    },
    "8025": {
      "label": "Mailhog UI",
      "visibility": "public"
    }
  }
}
